{
  "name": "pubflow-native",
  "private": true,
  "version": "0.1.0",
  "description": "Pubflow Native — fullstack TypeScript (React pages + Hono API in one process).",
  "workspaces": [
    "library",
    "starter",
    "examples/minimal",
    "examples/custom-hono-server"
  ],
  "scripts": {
    "test": "bun test library/src && bun run test:smoke",
    "test:unit": "bun test library/src",
    "test:smoke": "bun scripts/smoke.mjs",
    "typecheck": "bun run --cwd library typecheck"
  }
}
